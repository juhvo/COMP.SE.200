{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "Software testing assignment",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "jest",
    "coverage": "c8 --src src --reporter lcov --exclude src/.internal --exclude test npm test",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.16.0",
    "c8": "^7.10.0",
    "coveralls": "^3.1.1",
    "jest": "^27.3.1"
  }
}
